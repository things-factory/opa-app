const initMenus = require('./menus')
const initCodes = require('./codes')
const initRoles = require('./roles')
const initPrivileges = require('./privileges')
const initSettings = require('./settings')

module.exports = {
  initMenus,
  initCodes,
  initRoles,
  initPrivileges,
  initSettings
}
